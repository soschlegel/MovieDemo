projection;
strict ( 2 );
use draft;

define behavior for ZMO_C_MOVIE_ADMIN_D alias MovieDraftAdmin
{
  use create;
  use update;
  use delete;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}